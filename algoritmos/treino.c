#include <stdio.h>
#include <stdlib.h> 

int main() {


    //exercicio 5 
    /*
    int v[5];
    int *p; 
    for (int i = 0; i < 5; i++) {
        scanf("%d", &v[i]); 
    }

    p = v;

    for (int i = 0; i < 5; i++) {
        if(v[i] % 2 == 0){
            p = &v[i];
            printf("%d\n", p);
        }
        
    }
    
    return 0; 
}
*/
























    // exercicio 4 
    /*
    int v[5];
    int *p; 
    for (int i = 0; i < 5; i++) {
        scanf("%d", &v[i]); 
    }

    p = v;

    for (int i = 0; i < 5; i++) {
        (*p) = (*p) * 2; 
        printf("%d ", v[i]); 
        p++;
    }
    
    return 0; 
}
*/

    //exercicio 3 
    /*
    float m[3][3];
    int *p;
    for(int i = 0; i < 3; i++){
        for(int j = 0; j < 3; j++){
            scanf("%f", m);
            p = &m[i][j];
            printf("%d\n", p);
        }
    }
*/

    // exercicio 4 

}
