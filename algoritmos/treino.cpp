#include<bits/stdc++.h>
using namespace std;

int gcd(int a, int b)
{
	float F, K;
    for(int C=-10; C<=100; C++){
        F = 9 * C / 5.0 + 32;
        K = C + 273,15;
        cout << "F: " << F << "K: " << K << endl; 
    }

}